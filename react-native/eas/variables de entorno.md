la forma segura para datos sensible es:

```bash
eas secret:create --scope project --name EXPO_PUBLIC_TURSO_URL --value "libsql://app-database-elvisvillegas.aws-us-east-1.turso.io"
```

luego seleccionar string

la opcion file es para config mas complejas, como arhivos .pem 
